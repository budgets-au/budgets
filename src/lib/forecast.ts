/**
 * Number of future occurrences/periods to project for a scheduled
 * transaction or budget. Shared between the upcoming-forecast list and
 * the chart's forward bars so they stay in lockstep.
 */
export const FORECAST_HORIZON = 3;
